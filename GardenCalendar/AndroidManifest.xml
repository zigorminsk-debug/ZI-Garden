<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.csl.gardener"
    android:versionCode="17"
    android:versionName="2.5">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <!-- android:label у launcher-Activity задаёт подпись ярлыка на рабочем столе — аббревиатура -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PlantsActivity" android:exported="false" android:label="@string/plants" />
        <activity android:name=".DiseaseActivity" android:exported="false" android:label="Справочник болезней" />
        <activity android:name=".TasksActivity" android:exported="false" android:label="@string/tasks" />
        <activity android:name=".CalendarActivity" android:exported="false" android:label="@string/calendar" />
        <activity android:name=".MaterialsActivity" android:exported="false" android:label="@string/materials" />
        <activity android:name=".SoilActivity" android:exported="false" android:label="Почва и плодородие" />
        <activity android:name=".SettingsActivity" android:exported="false" android:label="@string/settings" />
        <activity android:name=".VisitCalendarActivity" android:exported="false" android:label="Календарь посещения" />
        <activity android:name=".AboutActivity" android:exported="false" android:label="@string/about" />
        <activity android:name=".JournalActivity" android:exported="false" android:label="Журнал сада" />
        <!-- Принимает тап по .json-вложению из Gmail/проводника: предлагает восстановить данные. -->
        <activity
            android:name=".ImportBackupActivity"
            android:exported="true"
            android:label="ZI Garden — импорт данных"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:host="*" android:mimeType="application/json" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" android:host="*" android:mimeType="application/json" />
            </intent-filter>
        </activity>

        <!-- Выбор своей фотографии культуры через системный файловый диалог. -->
        <activity
            android:name=".PhotoPickActivity"
            android:exported="false"
            android:label="Фото культуры"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <service android:name=".RescheduleService" android:exported="false" />

        <!-- Провайдер для выдачи скачанного APK установщику (самообновление) -->
        <provider
            android:name=".ApkProvider"
            android:authorities="by.csl.gardener.apk"
            android:exported="false"
            android:grantUriPermissions="true" />

        <receiver android:name=".AlarmReceiver" android:exported="false" />
        <receiver android:name=".NotifyTapReceiver" android:exported="false" />
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
        <!-- Виджет «Задачи на сегодня» на рабочем столе -->
        <receiver
            android:name=".TasksWidgetProvider"
            android:exported="true"
            android:label="ZI Garden — задачи">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>
        <!-- Виджет «Погода для сада» -->
        <receiver
            android:name=".WeatherWidgetProvider"
            android:exported="true"
            android:label="ZI Garden — погода">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_weather_info" />
        </receiver>
        <!-- Виджет «Активно в этом месяце» -->
        <receiver
            android:name=".SeasonWidgetProvider"
            android:exported="true"
            android:label="ZI Garden — сезонные угрозы">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_season_info" />
        </receiver>
        <!-- Виджет «Садовая памятка дня» -->
        <receiver
            android:name=".TipWidgetProvider"
            android:exported="true"
            android:label="ZI Garden — памятка дня">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_tip_info" />
        </receiver>
    </application>
</manifest>
